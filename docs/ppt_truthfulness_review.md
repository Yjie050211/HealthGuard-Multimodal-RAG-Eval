# PPT 逐页真实性审计与修改建议

审计对象：`娄有杰_面试展示PPT临时版.pptx`

## 总体结论

PPT 的教育背景、竞赛经历、研究兴趣类内容不在本次代码审计范围内。本次重点审计第 4、5、8 页中关于 CareEvent 项目的技术表述。

当前代码可以支撑的项目定位是：

> 面向健康监护场景，完成了一个文本化异常事件样例、结构化大模型输出回放、本地健康知识检索、引用回答、基础指标计算和失败案例分析的最小可复现原型。

当前代码不能支撑的表述是：

> 已完成 ST-GCN baseline、完整 GraphRAG 系统、动态图/分布式图计算、大规模对比实验或成熟技术报告。

## 第 1 页：封面

风险：低。

建议：不涉及项目技术声明，可保留。

## 第 2 页：目录

风险：低。

建议：可保留“国家级大创：异常理解与可信问答评测”，但展示时说明项目是阶段性原型。

## 第 3 页：教育背景

风险：非本项目代码范围。

建议：如成绩、排名、奖项均有证明材料，可保留。

## 第 4 页：项目经历

### 原表述风险

- “知识检索 LightRAG / GraphRAG 思路”：可保留“思路”，不能讲成系统实现。
- “准确率 / 误报率 / 延迟 / 一致性”：现在可讲，但要说明是 10 条模拟文本样例、小规模离线复算。
- “模型对比：以 SVM、ST-GCN 等作为 baseline”：当前只支持 SVM 小规模二分类流程；ST-GCN 不支持。
- “已沉淀对比实验记录、失败案例分析与技术报告初稿”：当前只有对比实验设计、真实失败案例和 docs，不宜说完整技术报告。

### 建议替换

```text
方法：先构建 10 条模拟健康监护事件样例，使用已保存的大模型结构化输出，结合本地健康知识库检索，形成异常类型、证据、风险等级、建议和知识来源的回答。
```

```text
当前结果：已跑通离线最小闭环，生成 predictions.jsonl、metrics.json、latency.csv、failure_cases.md 和 confusion_matrix.png；SVM 已完成 scikit-learn 小规模二分类流程验证，ST-GCN 作为后续补充基线。
```

```text
真实边界：GraphRAG 目前是知识组织思路探索，尚未实现图构建和图检索。
```

## 第 5 页：思考总结

### 原表述风险

- “已有积累：ST-GCN 时空图 baseline”风险高。
- “图结构建模经验”容易被追问当前项目里的图在哪里。
- “动态图/增量/分布式图计算”应放在后续规划，不要暗示已做过。

### 建议替换

```text
已有积累：
- 健康监护异常事件评测字段设计
- RAG/GraphRAG 思路下的知识组织与可追溯回答探索
- 对 ST-GCN 时空图建模思想的调研
- Python/C++/Java 工程基础
```

```text
下一阶段延展：
- 完善 SVM 小规模基线
- 基于开源实现复现 ST-GCN 推理流程
- 学习图划分、增量计算和 GNN 系统论文
```

## 第 6 页：竞赛与实践

风险：非本项目代码范围。

建议：如果 DeepSeek 系统、MCM、竞赛材料可举证，可保留。不要把这些经历和 CareEvent 项目指标混在一起讲。

## 第 7 页：研究兴趣与未来规划

风险：低。

建议：“复现 1-2 个图算法 baseline”放在未来规划是合适的。注意不要说已经完成。

## 第 8 页：为什么选择许老师课题组

### 原表述风险

- “做过 ST-GCN 时空图 baseline”当前没有代码证据。
- “做过 RAG/GraphRAG 思路探索”中 GraphRAG 只能讲思路，不是系统。

### 建议替换

```text
我目前完成的是健康监护异常事件评测原型：包括文本化事件样例、结构化输出、健康知识检索、指标计算和失败案例分析。这个过程让我意识到结构化表示、证据链和可复现实验的重要性。
```

```text
ST-GCN 方面我主要完成了方法调研，理解其把人体关键点和时间帧组织成时空图的思想；后续希望基于开源实现完成小规模推理验证。
```

```text
GraphRAG 方面我当前只是参考其实体关系和知识组织思想，还没有实现完整图构建、图数据库或图检索。
```

## 第 9 页：致谢

风险：低。

建议：可保留。

## 可直接使用的稳妥项目表述

```text
这个项目目前是一个健康监护异常事件理解与可信问答评测原型。我现在已经完成了 10 条模拟文本样例、健康知识库、本地检索、结构化回答、基础指标计算、真实失败案例提取，以及一个 scikit-learn SVM 小规模二分类基线。项目还没有完成 ST-GCN 基线，也没有实现完整 GraphRAG；这些是我后续准备补齐的方向。
```

```text
对我来说，这个项目的训练价值主要不是声称模型效果很好，而是学会把一个智能应用问题拆成数据、字段、检索、回答、指标和误差分析，并把每一步做成可以复现和被追问的材料。
```
